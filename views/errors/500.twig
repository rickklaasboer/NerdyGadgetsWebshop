{% extends 'templates/error.twig' %}

{% block title %} {{ __('errors.500.title') }} {% endblock %}

{% block content %}
    <div class="container h-100 d-flex">
        <div class="mt-auto mb-auto">
            <div class="row flex-column justify-content-center">
                <div class="col-12 mb-2">
                    <img class="w-25" src="/assets/img/logo.png" alt="">
                    <hr>
                </div>
                <div class="col-12">
                    <h2 class="font-weight-bold">{{ __('errors.500.title') }}</h2>
                    <p>{{ __('errors.500.subtitle') }}</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}